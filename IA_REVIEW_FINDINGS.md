# Identity Authority port: review findings

Four adversarial reviewers over the four source slices, 2026-09-05.
All four returned NEEDS_FIXES. Counts: {"data-loss": 8, "correctness": 17, "style": 3}.

Draft code these refer to: `update/_ia_sources_draft.py` (not imported).


## Slice: canonical (NEEDS_FIXES)

### [data-loss] ed_people_canonical() and ed_sors_sor_record(), `out[row.entry_dn] = {...}` (slice not yet assembled into /Users/paulalbert/worktrees/reciterdb-ia/update/buildIdentityAuthority.py); consumed by plan_deletions() at buildIdentityAuthority.py:222

**Problem.** Switching the key from the SPL-reconstructed dn to row.entry_dn is right, but nothing migrates the existing rows. `_person_canonical.dn` and `_person_sor_record.dn` are populated today by `eval dn = "uid=" . uid . ",ou=people,dc=weill,dc=cornell,dc=edu"` (Splunk Queries - 2026-09-05.txt:139) and `eval dn = "uid=" . uid . ",ou=" . ou . ",ou=sors,dc=weill,dc=cornell,dc=edu"` (line 607). plan_deletions() computes `existing - live` where `existing` is the old-shaped DB dn set and `live` is the real entry DN set. The SOR reconstruction interpolates the *ou attribute value*, not the RDN, so any case or spelling difference makes the two shapes permanently unequal -- PORT_PLAN.md lists the affiliates branch as `ou=nyp affiliates,...` (lowercase) while the `ou` attribute plausibly reads `NYP Affiliates`. The author's own declared divergence adds that the reconstruction assumes every SOR branch is exactly one level under ou=sors, so any deeper entry already carries a wrong dn in the DB. Concrete failure: on the first live run every mismatched row is classified `gone`. If mismatches are under MAX_DELETE_FRACTION (2%) they are deleted (or soft-delete flagged) while the upsert simultaneously inserts a duplicate row under the real DN -- one live person, one tombstoned row and one new row. If mismatches exceed 2% the run raises SystemExit every night and the table silently stops being refreshed.

**Fix.** Before any live run, dump both key spaces and compare: `SELECT dn FROM _person_canonical` / `_person_sor_record` against the entry_dn set from one --dry-run, and report the exact-match rate per type. If it is not 100%, run a one-time UPDATE rewriting the stored dn to the real entry DN (matched on weillCornellEduCWID + uid, or CWID + SORID for SOR) before the first write. Regardless, force the first live run to --no-delete and gate DELETE_MODE on a measured 100% key overlap, not on the 2% ceiling -- the ceiling is sized to catch a bad feed, not a key-space change.

### [data-loss] ed_people_canonical(): `mail`, `street`, `telephoneNumber`, `weillCornellEduProviderID`, `labeledURIpops`/`labeledURIvivo`/`labeledURIonlinedirectory`. ed_sors_sor_record(): `mail`, `street`, `telephoneNumber`, `weillCornellEduStatus`, `weillCornellEduDegree`, `personalTitle`, `company` (`row.get("o;company")`). Premise stated in mv()'s docstring, buildIdentityAuthority.py:186-195.

**Problem.** The rule the slice is built on -- that the 39 `nomv` lines "declare exactly which attributes the original author knew were multi-valued" and that anything not on that list is safe for row.get() -- is wrong. `nomv` does not decide whether a set reaches the column, only the separator: it flattens a multivalue joined by newline, which is precisely why every `nomv` in the file is immediately followed by `rex mode=sed "s/\n/|/g"`. An attribute that is never nomv'd still reaches the lookup as a newline-joined multivalue and is pushed by dbxoutput as that string. For the SOR search this is not an inference about outputlookup semantics at all: `stats list(*) as * by dn` (Splunk Queries - 2026-09-05.txt:609) explicitly converts EVERY field to a multivalue immediately before `table` and `outputlookup`, so every SOR column in the DB today holds the full joined set wherever ED returned more than one value. Concrete failure: a person with mail = [jdoe@med.cornell.edu, jd2001@med.cornell.edu] has both in `_person_canonical.mail` today and gets only the first after the port; a SOR record with two weillCornellEduDegree values or two street lines loses the second. Same class as the bug that emptied the 15 person-type flags: one value into a column whose consumers expect the set, no row-count change to expose it.

**Fix.** Do not decide this from the SPL text. Run one LDAP pass over both branches recording, per attribute, max(len(row.all(attr))) across all entries, and log it; route every attribute whose max exceeds 1 through mv(), not just the nomv'd ones. Cross-check against the current DB with one query per table -- e.g. `SELECT COUNT(*) FROM _person_sor_record WHERE mail LIKE '%\n%' OR street LIKE '%\n%' OR weillCornellEduDegree LIKE '%\n%'` -- and if that count is non-zero, mv() is mandatory for those columns and the separator change (newline to pipe) needs declaring as its own divergence.

### [correctness] ed_people_canonical() and ed_sors_sor_record() docstrings, the dropped `where ... len(uid) < 20` / `len(uid) > 20`; SPL at Splunk Queries - 2026-09-05.txt:180, 189 and 611

**Problem.** The stated justification -- "a mirror-image pair that exists only to separate ou=people from ou=sors inside one shared lookup" -- is demonstrably false, so the filters' real purpose has not been established and dropping them is an undeclared behaviour change. There is no shared lookup: canonical writes `identity_authority_canonical` (line 150), SOR writes `identity_authority_sor_record` (line 614), and the only lookup the two do share, `identity_authority_dn`, carries just dn,type,source and no uid at all, so len(uid) could not separate populations there even in principle. The placement is not mirror-image either: canonical's `len(uid) < 20` lives in the two consumer searches that read the lookup ("Canonical, db update" line 189, "Canonical Recon" line 180), while SOR's `len(uid) > 20` lives in the builder itself (line 611) ahead of outputlookup. Concrete failure: any (objectClass=weillCornellEduPerson) entry under ou=people with a uid of 20+ characters is now inserted into _person_canonical although the Splunk job has always excluded it from the push, and any weillCornellEduSORRecord under ou=sors with a uid of 20 characters or fewer is now inserted into _person_sor_record although the builder has always dropped it before writing. Being inserts, no row-count diff flags them -- they read as the port simply producing more data.

**Fix.** Measure before dropping. Two counts from one --dry-run: entries under ou=people with len(uid) >= 20, and weillCornellEduSORRecord entries under ou=sors with len(uid) <= 20. If both are zero the filters really are dead and the divergence note should cite the measured zeros instead of the shared-lookup story. If either is non-zero, keep that filter (or state what the admitted rows are and why they belong in the table) before the first live write.

### [correctness] ed_sors_sor_record() -> weillCornellEduStartDate / weillCornellEduEndDate / weillCornellEduDOB, read through _Row.get() rather than _ts()

**Problem.** _ts()'s own docstring identifies the hazard -- "_Row.get() normalises that to ISO-8601 WITH the offset ('...+00:00'), which is not the shape the column holds" -- and then routes only createTimestamp, modifyTimestamp and weillCornellEduHireDate through _ts(). weillCornellEduStartDate, weillCornellEduEndDate and weillCornellEduDOB are date-typed and go through _Row.get(). That ldap3 returns datetime for these specific attributes is established, not assumed: buildIdentity.py:1232 documents it and buildIdentity.py:601/790 depend on _flatten()'s isoformat() so that `[:4]` yields the year. Concrete failure: _person_sor_record.weillCornellEduStartDate receives `2019-07-01T00:00:00+00:00` while createTimestamp in the same row receives `2019-07-01 00:00:00`. If the column is typed DATE/DATETIME the insert is rejected or silently truncated; if VARCHAR, every downstream strptime / LEFT(col,10) / string comparison written against the Splunk-era shape breaks -- and the SOR search never sliced these fields, so the Splunk-era shape is whatever ldapfilter rendered, not this one.

**Fix.** Route every GeneralizedTime-syntax attribute through _ts() (or an explicit date-only variant for DOB), and confirm the target column types with one query -- `SELECT column_name, data_type FROM information_schema.columns WHERE table_name IN ('_person_sor_record','_person_canonical') AND column_name LIKE '%date%'` -- plus one sample of the existing values, so the port writes the shape already in the column rather than a third shape.

### [correctness] MIN_ROWS["sor_record"] = 20000, /Users/paulalbert/worktrees/reciterdb-ia/update/buildIdentityAuthority.py:127

**Problem.** sor_record carries the same floor as canonical although the two populations differ by a large multiple: canonical is one row per person (~33,388 per the identity port's own build log), while sor_record is one row per SOR record and every person has at least one, most several. A floor of 20000 therefore cannot catch a half-empty read of ou=sors -- it only catches a read that lost roughly 70-80%. Concrete failure: a paged search that ends early (a dropped paging cookie ends paged_search_generator with no result code, so raise_exceptions=True never fires) after 25,000 of ~90,000 SOR records passes the floor and is logged as a successful source. plan_deletions() would then trip MAX_DELETE_FRACTION and abort -- but --no-delete exists as a flag, and on that path the floor is the only guard, so a 70%-truncated snapshot is upserted and the run reports success.

**Fix.** Set each floor from a measured count rather than a shared round number: run the source once, log the count, and set the floor to ~0.8x it -- or persist the previous successful run's count in a build-log table and compare against that, which also supplies the staleness signal the handoff flags as missing on reciterdb.identity. At minimum raise sor_record's floor above canonical's, since it can never legitimately be smaller.

### [correctness] ed_people_canonical() and ed_sors_sor_record() attribute request lists -- weillCornellEduOrgUnit;level1/level2 and weillCornellEduPrimaryOrgUnit;level1/level2 absent

**Problem.** Adding orgUnit/orgUnitCode is one of the two stated drivers of this port (HANDOFF_2026-09-05_ia-port.md, "The ask"), and neither source requests any of the four attributes the sibling port already reads (buildIdentity.py:637-641, 720-721). Both read only weillCornellEduDepartment / weillCornellEduDepartmentCode / weillCornellEduPrimaryDepartment / weillCornellEduPrimaryDepartmentCode. Concrete failure: the port ships without the feature it was commissioned for, and -- more consequentially -- there is no migration watch, so when ED retires the old attributes these columns go empty on a run that reports success and passes every guard here (row counts are unchanged, so MIN_ROWS and MAX_DELETE_FRACTION are both blind to it). The sibling port anticipated exactly this with _watch_orgunit_migration() logging old-vs-new coverage each run.

**Fix.** Add the four subtyped attributes to both request lists and port _watch_orgunit_migration() so each run logs old-vs-new population coverage even while the new columns are not yet emitted. When the columns are emitted, coalesce (new or old) rather than hard-switching -- the handoff's measurement is that zero records carry a new attribute without the old one, and that L1 is the parent org while L2 is the actual unit, so taking L1 alone misfiles everyone under their parent.

### [style] Module docstring ("See PORT_SPEC.md in wcmc-its/IdentityAuthorityDatabase", buildIdentityAuthority.py:36) and _active_member()'s docstring in the slice

**Problem.** PORT_SPEC.md does not exist -- not in /Users/paulalbert/Dropbox/GitHub/Identity Authority Database and not in /Users/paulalbert/worktrees/reciterdb-ia. Two comments cite it as authoritative, and _active_member() specifically defers to it for "the one-line query that settles which casing ED actually returns", which is the only thing standing between the declared weillCornellEduActiveMember rewrite and an unverified behaviour change on every row of the table. The measurement is therefore not merely unrun, it is untracked. (The rewrite itself checks out: with lowercase values `max(x) > "false"` and the case-insensitive any-true test agree exactly; with RFC 4517 uppercase the SPL is always 0.)

**Fix.** Write PORT_SPEC.md with the divergence list and the outstanding probes, or repoint both comments at PORT_PLAN.md and add the ActiveMember casing query to its Status checklist. Then run it -- one search for (weillCornellEduActiveMember=TRUE) against one for (weillCornellEduActiveMember=true), comparing counts -- and record the answer next to the divergence.


## Slice: roles-org (NEEDS_FIXES)

### [data-loss] slice ed_organization(), the "telephoneNumber" and "labeledURI" emit lines

**Problem.** Both are read with row.get(), which _Row flattens to the FIRST value only. Both are multi-valued by definition (RFC 4519 telephoneNumber, RFC 2079 labeledURI carry no SINGLE-VALUE) and neither is nomv'd in the SPL, so today they reach the lookup as Splunk mv fields carrying every value. This is the same class of bug the module header says emptied 15 person-type flags on the sibling port: no row-count change, so a row-count diff cannot catch it. It is also internally inconsistent -- the function's own comment four lines above states the policy ("cn, uid, seeAlso and o are multi-valued in ED but the SPL only nomv's some of its multi-values ... Joining them keeps every value, which is the point of mv()") and then abandons it for these two.

**Fix.** Use mv(row, "telephoneNumber") and mv(row, "labeledURI"). The change is free even if ED turns out to hold one value per org unit: "|".join(["x"]) == "x", so mv() and get() are byte-identical on single-valued data. Concrete failure without it: an org unit with a main line and a fax, or with both a department site and a directory URL, silently loses the second value in _organization.telephoneNumber / .labeledURI on every run.

### [correctness] /Users/paulalbert/worktrees/reciterdb-ia/update/buildIdentityAuthority.py:134 and :139 (MIN_ROWS)

**Problem.** Neither floor can catch a large partial read on this slice's two sources. sor_role_record is given 20000, the SAME floor as canonical and sor_record, even though the slice's own docstring calls it "The largest ED read in this job" and PORT_SPEC.md line 253 calls it "the highest-cardinality object in the pipeline ... Every person has one SOR record but typically several role records". If people are ~30k (implied by the canonical floor), role records are 100k+, so the floor sits at roughly 20% of the population: a read that loses 75% of the branch passes the guard silently. organization is given 100, which is a floor against "returned 12", not against a subtree that returns 150 of several thousand org units. Because both are full-snapshot sources feeding a set-difference deletion, a short-but-above-floor read is exactly the input that makes plan_deletions() nominate live rows, and under --no-delete it silently upserts a partial snapshot with no signal at all.

**Fix.** Calibrate each floor to that type's own observed population, not to the people count: set sor_role_record from one COUNT(*) on _person_sor_role_record and organization from COUNT(*) on _organization, at ~80-90% of the live number, and record the measured baseline in a comment next to the value. Better still, adopt buildIdentity.py's MIN_ROWS_FLOOR pattern (line 157, staged < baseline * 0.95) so the floor is relative to the previous run rather than a hand-typed constant that ages.

### [correctness] /Users/paulalbert/worktrees/reciterdb-ia/update/buildIdentityAuthority.py:340

**Problem.** The unresolved-base-DN warning is dead code and can never fire. UNRESOLVED_ALIASES holds ALIAS names ({"ed-groups", "ed-locations", "ed-phones", "ed-emails", "ed"}) while {fn.type_name for fn in SOURCES.values()} holds TYPE names ({"sor_role_record", "organization", ...}). The two sets are drawn from disjoint vocabularies, so the intersection is empty for every possible registration and logger.warning is unreachable. This slice is precisely the case the guard exists for: ed_organization() reads alias "ed-groups", whose base DN is an unproven candidate (BASE_DN line 100, flagged "UNRESOLVED - candidates only"). Concrete failure: ou=Groups,dc=weill,dc=cornell,dc=edu binds but is the wrong container (or a partially populated one), the read returns 150 rows, the organization floor of 100 passes it, and the operator gets no warning anywhere in the log that the base DN was never resolved.

**Fix.** Bind the check to the alias actually used, not to the type name. Simplest: warn inside ldap_search() -- `if alias in UNRESOLVED_ALIASES: logger.warning("alias %s is an UNRESOLVED candidate base: %s", alias, base)` -- so it fires once per real search. Alternatively record the alias on the source (extend @source to take it, set fn.alias) and intersect on {fn.alias for fn in SOURCES.values()}.

### [correctness] slice ed_sor_role_record(), the six weillCornellEduOrgUnit* / weillCornellEduOrgUnitCode* emit lines

**Problem.** Six columns are emitted that do not exist in _person_sor_role_record: the SPL's table clause (line 703 of the export) ends at createTimestamp/modifyTimestamp and names none of them. The code comment acknowledges this ("These need DDL before the first live run") but nothing in the module enforces it -- there is no schema assertion, no allow-list of writable columns, and no --dry-run check that compares the emitted key set against the target table. Concrete failure on assembly: the first live run either dies inside the writer on an unknown column, or, if the writer intersects the dict against the table's real columns, drops all six silently and the migration they exist to measure produces nothing while reporting success.

**Fix.** Make the prerequisite executable rather than a comment: add the six columns to the DDL before assembly, and have the writer (or a startup check) read the target table's column list once and abort with the missing names if any emitted key is absent. Keeping the six columns is right -- they are the measurable half of the department migration and _dept_value()'s L2-or-L1-or-department rule depends on them -- but they must not be able to fail quietly.


## Slice: contact (NEEDS_FIXES)

### [data-loss] ed_phones_contact(), `"telephoneNumber": r.get("telephoneNumber") or r.get("mobile")`

**Problem.** This is the priority-1 pattern, reached via `or` instead of a bare get(). The function's own docstring asserts that a single ED entry carries both types — "LDAP returns an entry from BOTH arms of the SPL's two-branch search when it carries a desk number and a mobile" — which is precisely why weillCornellEduType is read with mv(). But the declared divergence justifies the entry_dn key with the opposite claim: "the desk entry and the mobile entry are different LDAP entries with different DNs". Both cannot be true. If the docstring's premise holds, entry_dn does NOT split them (it is one entry, one DN), and the `or` writes the desk number while silently discarding the mobile — one row, one column, no row-count change, so the row-count diff the author promises ("expect _contact_phone to gain rows") cannot detect it. Secondarily, telephoneNumber is multi-valued in LDAP (RFC 4519), so get() takes only the first of several desk numbers even when the premise does not hold. The SPL is no defence here: its two branches produced two rows that then collided on one _key, so both implementations lose the number — the port just claims to have fixed it and has not.

**Fix.** First measure the premise: run `(&(objectClass=*)(telephoneNumber=*)(mobile=*))` and `(&(weillCornellEduType=telephoneNumber)(weillCornellEduType=mobile))` against ed-phones and report the counts. If either is non-zero, stop merging the two attributes into one column — emit `mv(r, "telephoneNumber")` and `mv(r, "mobile")` into separate columns, or pipe-join them (`"|".join(r.all("telephoneNumber") + r.all("mobile"))`) so the column matches the joined-set shape the other nomv'd columns already use. Either way, correct the docstring so it states one premise, not two contradictory ones.

### [data-loss] All four sources — the `{r.entry_dn: {...}}` key — plus plan_deletions() consuming db_dns read from `_contact_email.dn` / `_contact_phone.dn` / `_contact_location.dn` / `_location.dn`

**Problem.** Replacing the reconstructed dn with row.entry_dn silently re-keys four production tables, and nothing in the slice migrates the `dn` column those tables already hold. Every existing DB row's dn is an SPL reconstruction (`uid=<uid>,ou=emails,ou=contacts,...`, `uid=<uid>,ou=telephoneNumbers,ou=contacts,...`, `uid=<uid>,ou=locations,ou=contacts,...`, `cn=<cn>,ou=locations,ou=Groups,...`). The live set will hold real entry DNs. The two sets overlap only where a reconstruction happens to equal the real DN — provably not the case for at least one of every person's phone rows (two entries cannot both be `uid=x,ou=telephoneNumbers,...`), and unknown for the rest. So the deletion set difference lands at an unpredictable point between 0% and 100% of each table. Concrete failure path: first live run with deletion enabled, `plan_deletions` computes a fraction far above 0.02 and SystemExits every night; the operator raises MAX_DELETE_FRACTION to clear what looks like a tuning problem, and the next run deletes the bulk of all four contact tables. The benign-looking half is just as bad: until deletion runs, the upsert INSERTs a second copy of every entry under its new DN rather than UPDATEing the existing row, doubling the four tables. Neither consequence is mentioned in the declared divergences, which present the key change as purely additive ("Expect _contact_phone to gain rows").

**Fix.** Before the first run that has deletion enabled, migrate the `dn` column in `_contact_email`, `_contact_phone`, `_contact_location` and `_location` to real entry DNs — or add an `entry_dn` column, backfill it from a full ED read, and cut the DN-set-difference and every downstream consumer over to it. Add the migration as an explicit precondition in the module docstring's DN_FROM_ENTRY note, and keep DELETE_MODE="flag" until a dry run shows the per-type `gone` count near zero rather than near 100%.

### [correctness] main(), `unresolved = UNRESOLVED_ALIASES & {fn.type_name for fn in SOURCES.values()}`

**Problem.** Dead guard. UNRESOLVED_ALIASES holds alias names ('ed-groups','ed-locations','ed-phones','ed-emails','ed'); the set comprehension yields type names ('email','phone','location','location_master',...). The two namespaces never overlap, so the intersection is always empty and the warning never fires. Verified by running the intersection: `set()`. Every one of the four sources in this slice searches an alias whose base DN is an unproven guess, and a live run logs nothing to say so — the one warning standing between an operator and a run against three guessed base DNs is inert. The root cause is that @source records only type_name; the alias exists solely as a string literal inside each function body, so there is nothing correct to intersect against.

**Fix.** Record the alias on the source: `def source(type_name, alias=None)` setting `fn.alias`, called as `@source("location", alias="ed-locations")`, then `unresolved = UNRESOLVED_ALIASES & {fn.alias for fn in SOURCES.values() if fn.alias}`. Add a demo() assertion that the guard fires for at least one registered source while any alias remains unresolved, so it cannot silently die again.

### [correctness] BASE_DN["ed-locations"] / ["ed-emails"] / ["ed-phones"], and spike()

**Problem.** The candidate base DNs contradict the only evidence the authoritative SPL contains, and spike() cannot disprove them. The SPL's own reconstructions name the parent branches directly: line 228 `ou=emails,ou=contacts,ROOT`, line 296 `ou=locations,ou=contacts,ROOT`, line 520 `ou=telephoneNumbers,ou=contacts,ROOT`. The port's candidates are `ou=emails,ROOT`, `ou=locations,ou=Groups,ROOT` and `ou=phones,ROOT`. ed-locations is the dangerous one: `ou=locations,ou=Groups` is a real, populated branch — it is where Location Master lives (SPL line 331) — and the candidate was taken from buildIdentity.py's comment, which describes the Everbridge location *taxonomy* branch, not the person-contact branch. spike() searches `(objectClass=*)` with attrs ["cn"] and prints "ok" for anything that returns rows, so it will report `ed-locations ... ok` for the wrong branch, and the module docstring's promise that "--spike is what proves the base DNs" is not met. Concrete failure: ed_locations_contact() searches the Groups branch, `(weillCornellEduType=location)` matches no groupOfURLs entry, the source returns 0 rows, and run_sources aborts the entire nightly job — all eight sources, every night — on a floor breach that reads as an ED outage rather than a wrong base DN.

**Fix.** Set the three candidates from the SPL's own reconstructions: `ou=emails,ou=contacts,ROOT`, `ou=telephoneNumbers,ou=contacts,ROOT`, `ou=locations,ou=contacts,ROOT`. Then make spike() prove them: run each registered source's real filter against its real alias and print the row count, instead of `(objectClass=*)` — a branch that binds but returns zero rows for the source's filter must print FAILED, not ok.

### [correctness] MIN_ROWS["location_master"] = 100

**Problem.** The floor cannot catch a half-empty read on the one feed where a short read is destructive. The declared divergence rests on this floor ("MIN_ROWS['location_master'] = 100 makes run_sources() abort before anything is compared"), and that holds only for a zero-row read. It is 50x below the next-lowest floor (location, 5000) and is not derived from the population — every other floor in the dict plainly is. A read that returns 150 of N entries — a paging fault, a filter that stops matching after an ED schema change, a partially-populated replica — passes the floor, and location_master is precisely the feed whose SPL `outputlookup` has no append=t, i.e. the one the author identifies as able to nominate all of `_location` for deletion. MAX_DELETE_FRACTION is a real second net for that specific consequence, but it only turns the run into an abort; a short read still silently underwrites `_location` on any code path where the upsert runs before or independently of the deletion pass.

**Fix.** Derive the floor the way the others were: run `SELECT COUNT(*) FROM _location`, show the output, and set MIN_ROWS["location_master"] to roughly 80% of it. The comment above MIN_ROWS says the floors exist to catch "returned nothing / returned 12" — say so for this one too, or raise it to catch a half-read.

### [style] Module docstring line 28 ("See PORT_SPEC.md in wcmc-its/IdentityAuthorityDatabase") and the weillCornellEduPrimaryEntry comment in ed_emails_contact()

**Problem.** PORT_SPEC.md does not exist. `find` and `grep -r` over both the repo and update/ return only the citation itself; the repo holds PORT_PLAN.md and HANDOFF_2026-09-05_ia-port.md. This matters beyond a broken link because the citation is load-bearing: the sole justification for emitting weillCornellEduPrimaryEntry raw across three tables is "the port spec's normalisation of this attribute to a real boolean is proposed for sor_role_record only and explicitly leaves email/phone/location as an open decision". A reviewer cannot verify that the open decision exists, and neither could I. The same citation is the only record of which eight dead feeds are deliberately not reproduced.

**Fix.** Either write PORT_SPEC.md, or repoint both citations at PORT_PLAN.md and move the two claims (the PrimaryEntry decision, the eight dead feeds) into it. Verified separately and needing no change: the minute-vs-second timestamp divergence is safe — the only consumers that parse with "%Y-%m-%d %H:%M" are SPL lines 361 and 368, both of which this port deletes.


## Slice: dbside (NEEDS_FIXES)

### [data-loss] db_dns() SELECT + _merge_sql() ON clause + main() ordering (slice); vs. "Splunk Queries - 2026-09-05.txt" lines 132, 228, 296, 331, 520, 605, 675-679

**Problem.** The whole db side assumes the `dn` already stored in the IA tables is the same string ldap3 returns as `row.entry_dn`. It is not. Every SPL builder writes a RECONSTRUCTED dn: `"uid=" . uid . ",ou=emails,ou=contacts,dc=weill,..."` (line 228), `"uid=" . uid . ",ou=telephoneNumbers,ou=contacts,..."` (line 520), `"cn=" . cn . "," . seeAlso` / `",ou=departments,ou=Groups,..."` (lines 675-679). The module's own docstring concedes the Organization one "may have been writing wrong DNs for years," and the port's guessed BASE_DN entries contradict the SPL outright (port: `ou=emails,dc=weill,...`; SPL: `ou=emails,ou=contacts,dc=weill,...`; port: `ou=phones,...`; SPL: `ou=telephoneNumbers,ou=contacts,...`). Nothing in db_dns(), plan_deletions() or _merge_sql() reconciles the two forms. Confirmed: plan_deletions({'organization': {real_entry_dn: {}}}, {'organization': {spl_reconstructed_dn}}) -> "ABORT: 1 of 1 organization rows (100.0%) would be removed". Because main() calls plan_deletions() BEFORE upsert(), the first live run writes nothing at all, on every run, forever. And the flag an operator will reach for first, --no-delete, removes exactly that guard: plan={} and upsert() proceeds with `ON tgt.[dn] = src.[dn]` never matching, so `WHEN NOT MATCHED THEN INSERT` fires for every row and each affected table gets a full parallel copy under the new DN form. Consumers then see two rows per entity with no error and no row-count alarm on the ED side.

**Fix.** Prove the DN forms agree before any live write, and make the check independent of --no-delete. In db_dns(), after reading each table, compute the overlap against the built live set and abort if |live & existing| / |existing| is below ~0.9, with the message naming both a sample live DN and a sample DB DN (DNs are not on the sensitive-field list; mail/mobile/postalCode are). Run that check even under --no-delete, since --no-delete is the path that silently duplicates. If the forms genuinely differ, migrate the `dn` column (or add a normalized join key) before enabling either upsert or deletion.

### [data-loss] db_dns(): `SELECT [dn] FROM [table] WHERE [dn] IS NOT NULL`

**Problem.** The read does not exclude rows already soft-deleted by a previous run, so a flagged row stays in `existing` forever while being permanently absent from `live`. `gone = existing - live` is therefore CUMULATIVE, and MAX_DELETE_FRACTION (0.02) stops being a per-run ceiling and becomes a lifetime budget. Simulated on a 30,000-row `_contact_email` with 0.4% ED churn per run: run 2 = 0.80%, run 4 = 1.60%, run 6 = 2.40% -> plan_deletions raises SystemExit. Since main() runs plan_deletions() before upsert(), that abort stops the entire job -- the upsert included -- on run 6 and on every run after it. The handoff notes there is no staleness alert on this lane, so a job that has stopped writing raises nothing.

**Fix.** Exclude already-flagged rows from the reconcile read: `SELECT [dn] FROM [t] WHERE [dn] IS NOT NULL AND ([deleted] IS NULL OR [deleted] = 0)` (guarded on the column existing, same INFORMATION_SCHEMA check _delete_marker already does). Alternatively compute the ceiling against newly-gone DNs only. Either way MAX_DELETE_FRACTION must measure one run's churn, not the table's history.

### [data-loss] _columns_for() / _merge_sql() SET clause vs. _flag_sql()

**Problem.** DELETE_FLAG_COLUMN is written by _flag_sql() but is never in the MERGE's SET list, because _columns_for() only ever sees ED-sourced column names. Verified: _merge_sql('_contact_email', ['mail','ou'], 1) produces SET targets ['tgt.[mail]', 'tgt.[ou]'] only. So a soft-deleted DN can never be un-flagged. Concrete: an ED read blip drops 0.5% of email entries while the source still clears its MIN_ROWS floor (10000); plan_deletions sees 0.5% < 2% and flags them; next run they are back in ED, the MERGE happily updates mail/ou for those exact rows, and `deleted` stays set. Every consumer filtering on the flag loses those people permanently, with no row-count change on either side to catch it -- the same silent shape as the person-type bug the module was written to avoid.

**Fix.** Add an unconditional clear of the flag for matched rows: append `tgt.[deleted] = NULL` (or `= 0`) to the MERGE SET clause whenever the column exists -- presence in ED this run is exactly the evidence that the row is not deleted. Deliberately NOT COALESCE'd; this is the one column where a NULL from the source must win. Add the corresponding demo() assertion.

### [correctness] plan_deletions() set difference vs. _merge_sql() `ON tgt.[dn] = src.[dn]`

**Problem.** The two halves of the run disagree on string equality. Python's `existing - live` is case- and whitespace-sensitive; SQL Server's default collation (SQL_Latin1_General_CP1_CI_AS) is case- and trailing-space-insensitive. A DN that differs only in case matches in SQL and does not match in Python. Verified: with 200 live DNs and one DB row stored as `UID=7,ou=people,...` instead of `uid=7,...`, plan_deletions returns ['UID=7,ou=people,dc=weill,dc=cornell,dc=edu'] (0.50%, under the ceiling, so no abort). In the same run the MERGE's ON clause matches that row case-insensitively and UPDATEs it, and then apply_deletions() flags it deleted. One run both refreshes and soft-deletes the same live person. This is a distinct mechanism from the DN-form finding above and survives its fix.

**Fix.** Normalize both sides of the comparison to a single canonical key -- e.g. build `{dn.strip().casefold(): dn}` in db_dns() and key `built` the same way -- and do the set difference on the normalized keys while carrying the original DN string through to the SQL. Assert it in demo() with a case-only-divergent pair.

### [correctness] db_conn(): `database=os.environ.get("IA_DB_NAME", "IdentityAuthority")` and `IA_DB_ENV = ("IA_DB_URL", "IA_DB_USERNAME", "IA_DB_PASSWORD")`

**Problem.** IA_DB_NAME is named in the abort message but is not in IA_DB_ENV, so it is never required -- and `_mssql_target()` (buildIdentity.py:324) discards everything after the first `;`, which is precisely where a JDBC URL carries `databaseName=`. Verified: _mssql_target('jdbc:sqlserver://ia-prod.med.cornell.edu:1433;databaseName=IdentityAuthorityPrd') -> ('ia-prod.med.cornell.edu', 1433); the database name the operator supplied is thrown away and the hardcoded guess 'IdentityAuthority' is used instead. The abort message explicitly instructs the operator to build IA_DB_URL "from Splunk's Identity_Authority DB Connect connection" -- i.e. to paste exactly such a JDBC URL. If a database literally named IdentityAuthority exists on that server (a restore, a dev copy), the run connects to it, db_dns() logs plausible counts, and a full snapshot is MERGEd into the wrong database while every log line reports success. The comment above IA_DB_ENV says writing IA rows into the wrong server "would be far worse than refusing to start", but nothing actually verifies the target.

**Fix.** Add IA_DB_NAME to IA_DB_ENV so it is a required secret, or parse `databaseName=` out of IA_DB_URL when present and refuse when the two disagree. Then log `SELECT DB_NAME(), @@SERVERNAME, SUSER_SNAME()` once at the top of db_dns() so the target appears in the run log.

### [correctness] db_dns() -- counts are logged but never asserted; plan_deletions() `if not existing: continue`

**Problem.** Hazard #3 on the DB side is unguarded. Sources have MIN_ROWS; the DB read has no floor at all. db_dns() logs `rows=%d distinct=%d` and moves on whatever the number is, and plan_deletions() treats a zero-row DB side as benign ("db side empty, nothing to reconcile"). A wholly failed or empty read is therefore indistinguishable from a fresh table, and combined with the finding above it is the only thing that could have caught a connection pointed at an empty copy of the schema. It also means a half-empty read (say `_person_canonical` returning 4,000 of 33,000 rows) passes silently: it suppresses real deletions and hides that the read was truncated.

**Fix.** Give db_dns() a per-type floor -- reuse MIN_ROWS, or a DB_MIN_ROWS keyed the same way -- and raise SystemExit naming the table and both counts when a read falls under it. Treat `existing` empty as an abort unless an explicit IA_ALLOW_EMPTY_TABLE=<type> opt-in is set for a genuinely new table.

### [correctness] _merge_params(): `[_null_if_blank(row.get(c)) for c in columns]`

**Problem.** This is the one call in the slice that does the exact thing the module's header warns about at lines 67-73 ("a plain .get() silently emptied 15 person-type flags on the sibling port and was invisible until a production diff"), and it has no guard. It is safe only while every source returns plain dicts. _Row is a dict subclass, so one source returning `{dn: _Row(...)}` -- or building its row dict by copying a _Row -- passes every check here and every assertion in demo(). Verified against the real _Row: a plain dict carrying 'academic|academic-faculty|affiliate' binds that whole string, while the identical _Row binds 'academic'. _columns_for() even accepts the lowercased _Row keys without complaint, and SQL Server identifiers are case-insensitive so the column names still resolve. Result: one value written into a column whose consumers expect the pipe-joined set, no row-count change, invisible to a row-count diff.

**Fix.** Refuse ambiguous row objects at the boundary. In upsert(), before building params: `bad = [dn for dn, r in items if type(r) is not dict]` and SystemExit naming the source and type if non-empty. Or bind with `row[c] if c in row else None` on a plain-dict-only contract. Add a demo() assertion that a _Row-valued built dict aborts.

### [correctness] main() (new): `unresolved = UNRESOLVED_ALIASES & {fn.type_name for fn in SOURCES.values()}`

**Problem.** This intersects LDAP alias names with type names, which are disjoint by construction, so the warning can never fire. Verified: UNRESOLVED_ALIASES = {'ed','ed-emails','ed-groups','ed-locations','ed-phones'}; type names = {'canonical','email','location','location_master','organization','phone','sor_record','sor_role_record'}; intersection = set(). The run therefore proceeds against five guessed base DNs with no warning whatsoever -- and the SPL's own dn reconstructions show at least three of those guesses are wrong (`ou=emails,ou=contacts` not `ou=emails`; `ou=telephoneNumbers,ou=contacts` not `ou=phones`; `ed-locations` is used for two different subtrees, `ou=locations,ou=contacts` at line 296 and `ou=locations,ou=Groups` at line 331). The declared divergence says the new main() "reproduces the --demo/--spike paths verbatim", which is true -- but it reproduces a broken guard verbatim, and this block is the last chance to fix it.

**Fix.** Record the alias on the source (`fn.alias = alias` in the @source decorator, or a module-level SOURCE_ALIASES map) and intersect on that. Better: make it an abort rather than a warning for a live (non --dry-run) run, since a guessed base DN that binds to the wrong subtree produces wrong DNs, which is the failure in the first finding.

### [correctness] apply_deletions(): `marker = _delete_marker(cur, table) if DELETE_MODE == "flag" else None` sits below the `if dry_run: ... continue` branch

**Problem.** _delete_marker() is the only thing that verifies DELETE_FLAG_COLUMN exists, and it is unreachable under --dry-run. So a green --dry-run in flag mode proves nothing about the column whose name the code itself calls a guess. The live run then aborts inside apply_deletions() -- after upsert() has already committed -- with a message about a column nobody verified. This is hazard #6 from the identity port ("three green dry runs passed while the SQL was malformed") reproduced in the very function whose docstring cites it as the reason the delete branch refuses in dry-run too.

**Fix.** Move the INFORMATION_SCHEMA check ahead of the dry_run branch and run it in both modes: open a read-only connection under --dry-run (db_dns() already requires one in that path, so no new credential requirement) and call _delete_marker() for every table in the plan before logging the statement. Report the resolved marker value and its declared type in the dry-run log.

### [correctness] _merge_sql(): `row_ph` CASTs all of `cols`, key included; plus `WITH (HOLDLOCK)` inside upsert()'s single all-tables transaction

**Problem.** The CAST is justified for value columns (a bare NULL in a VALUES constructor really does type as int), but the key column can never be NULL -- it is the dict key from `built` -- so the rationale does not apply to it, and casting it costs the join. `ON tgt.[dn] = CAST(... AS NVARCHAR(MAX))` compares against a LOB-typed expression; if the stored `dn` is `varchar`, nvarchar's higher precedence forces CONVERT_IMPLICIT on the column and the index seek is lost outright. That matters because _batch_size() is small: verified 43 rows/batch at 45 columns, so `_person_sor_record` at ~40k rows is ~931 separate MERGE statements, `_person_canonical` ~688, `_person_sor_role_record` ~704. Each of those potentially scans its table, and all of them run under HOLDLOCK (serializable range locks) inside one transaction that spans all eight tables for the entire run. Concrete failure: the 600s `timeout` in db_conn() expires mid-run, or the IA database's readers block for the full duration of the job. (MERGE_MAX_ROWS = 1000 is also dead -- `2000 // (cols + 1)` is always the binding limit.)

**Fix.** Do not CAST the key: build the row constructor as `%s` for column 0 and `CAST(%s AS NVARCHAR(MAX))` for the rest, so the ON clause stays SARGable, and assert the shape in demo(). Separately, scope the transaction per table (commit after each type, log which types committed) so a failure on table eight does not hold locks over the whole database while tables one to seven wait -- the all-or-nothing property is worth stating explicitly as per-table if that is what it becomes.

### [style] upsert() and apply_deletions(): `except Exception:` around bodies that `raise SystemExit(...)`

**Problem.** SystemExit derives from BaseException, not Exception (verified: issubclass(SystemExit, Exception) is False), so the unmapped-type abort and _delete_marker()'s missing-column abort skip the rollback branch entirely. The transaction is still discarded when conn.close() runs in the finally, so no data is lost -- but the log line 'upsert rolled back -- no table was modified' never prints on those paths, and that line is what an operator reads to decide whether anything landed.

**Fix.** Use `except BaseException:` for the rollback-and-log block (re-raising unchanged), or add an explicit `except SystemExit:` arm alongside it.


# Round two: fix attempt and re-verification

All four slices still NEEDS_FIXES. 28 unresolved, **14 regressions**.
Regressions are the headline: the fix round broke things the draft had right.


## Slice: canonical (NEEDS_FIXES)

### Regressions introduced by the fix round

- _ts() now takes the entire eight-source job down on ONE bad value. The draft read weillCornellEduStartDate/EndDate/DOB through _Row.get() and wrote whatever came back -- wrong shape, but the run finished and the other seven tables were refreshed. The fixed _ts() raises SystemExit from inside ed_sors_sor_record() on any value that will not render positionally, and there is no env override (unlike UID_LEN_FILTER and IA_MIN_ROWS_SOR_RECORD, which both got one). Verified offline: '0' -> SystemExit, 'N/A' -> SystemExit, '2019' -> SystemExit, 'unknown' -> SystemExit. StartDate/EndDate are SOR-feed-populated, not operational, so one malformed value among ~100k SOR entries stops canonical, sor_record, sor_role_record, organization, email, phone, location and location_master, every night, until ED is corrected or the code is patched.

- The fix round emits eight columns that exist in neither target table; the draft emitted none. The draft's canonical emitted exactly the 39 columns of the SPL table clause at 'Splunk Queries - 2026-09-05.txt':144 and its sor_record exactly the 48 at :609 minus the four declared-dead ones. The fixed slice adds _PERSON_ORGUNIT_COLUMNS to both dicts. grep over the authoritative SPL returns 0 hits for OrgUnitLevel/OrgUnitCodeLevel and 3 hits for weillCornellEduOrgUnit, all of them the objectClass in the Organization searches (L403, L473, L481) -- the attributes are never read, so the columns cannot exist. _columns_for() (/Users/paulalbert/worktrees/reciterdb-ia/update/_ia_sources_draft.py:983) unions the emitted keys straight into the MERGE, and upsert()'s dry-run branch (:1088) only logs the SQL text, so --dry-run stays green and the first live run dies on SQL Server error 207 inside one transaction spanning all eight tables. The review's own fix for this finding said to log coverage 'even while the new columns are not yet emitted' -- the fix did the opposite.

- The new sor_record floor breaks the module's stated architecture invariant. /Users/paulalbert/worktrees/reciterdb-ia/update/buildIdentityAuthority.py:46-47 declares guard #1 as 'No shared mutable store, no ordering dependency between sources.' _PERSON_ROW_COUNTS is a module-global mutable store written by one source and read by another; it works only because run_sources() iterates sorted(SOURCES.items()) and 'ed_people_canonical' happens to sort before 'ed_sors_sor_record'. Rename either function and the guard silently self-disables -- announced at logger.info, not logger.warning. The comment's claim that this 'is not an ordering dependency between sources' is contradicted by the code that reads it.

### Still unresolved

- **[data-loss]** Finding 1 (the entry_dn re-key with no migration of the stored SPL-reconstructed dn) is answered in three places -- ed_people_canonical()'s docstring ('assert_dn_overlap() does that at runtime, ahead of every write, and aborts below 90%'), ed_sors_sor_record()'s docstring ('assert_dn_overlap() measures that before anything is written') and the author's DEFERRED note ('converts the hazard into a loud abort before any write'). assert_dn_overlap() is dead code. It is defined at /Users/paulalbert/worktrees/reciterdb-ia/update/buildIdentityAuthority.py:255; an AST scan of that file finds calls only at lines 371 and 377, both inside demo(). The assembled main() in /Users/paulalbert/worktrees/reciterdb-ia/update/_ia_sources_draft.py:1326-1361 runs run_sources -> db_dns -> plan_deletions -> upsert -> apply_deletions and never calls it; the scaffolding's own main() (buildIdentityAuthority.py:407-431) does not either. Failure scenario: first live run, _person_sor_record.dn holds 'uid=<uid>,ou=' + the ou ATTRIBUTE VALUE + ',ou=sors,...' (SPL L605) while live keys are real entry DNs, and nothing checks the overlap. On the default path plan_deletions computes a fraction far above 0.02 and SystemExits every night, so the table stops being refreshed with no staleness alert; under --no-delete plan={} and the MERGE's ON tgt.[dn]=src.[dn] never matches, so WHEN NOT MATCHED INSERT fires for every row and _person_sor_record gets a full parallel copy under the new DN form. Both outcomes the finding described, unmitigated.
  - Fix: Either wire assert_dn_overlap(built, existing) into main() between db_dns() and upsert() -- it must run under --dry-run and --no-delete both -- or delete the three docstring claims that say it already runs. Raise it against the dbside slice as a blocker rather than leaving canonical asserting a protection only demo() exercises.

- **[correctness]** The eight _PERSON_ORGUNIT_COLUMNS reach the MERGE and no code path can catch it before the live write. The slice's mitigation is one logger.warning per source. Failure scenario: DDL is not applied (the warning is one line among the ldap/source/orgunit/cardinality lines in a cron log); --dry-run passes because upsert() logs _merge_sql() without executing it; the live run reaches cur.execute() on _person_canonical, SQL Server raises 207 Invalid column name 'weillCornellEduOrgUnitLevel1', the except Exception arm rolls back the single transaction covering all eight tables, and nothing is written -- every night. This is hazard #6 the module cites verbatim ('three green dry runs passed while the SQL was malformed'), reproduced by the fix meant to close a different finding.
  - Fix: Until the DDL lands, request the twelve attributes and let _log_cardinality and the orgunit counts do the measurement the finding actually asked for, but keep the eight keys out of the emitted dict. If they must be emitted now, make the prerequisite executable: read INFORMATION_SCHEMA.COLUMNS once at startup for both tables and SystemExit naming any emitted key that is not a real column -- and run that check under --dry-run, where it costs nothing.

- **[correctness]** _ts()'s shape check is positional, not semantic, while the docstring and the author's claim present it as a guarantee ('refuses to emit anything other than YYYY-MM-DD HH:MM:SS', 'any value that will not render ... raises SystemExit'). It checks digit-ness and separator positions only. Verified by running it: '00000000000000Z' -> '0000-00-00 00:00:00' (passes), '20191345123456Z' -> '2019-13-45 12:34:56' (passes), '20190229000000Z' -> '2019-02-29 00:00:00' (passes; 2019 is not a leap year). Failure scenario: a SOR feed writes the common all-zero null-date sentinel into weillCornellEduEndDate; _ts() passes it; _null_if_blank() leaves it non-blank; the MERGE binds '0000-00-00 00:00:00' to a DATETIME column and SQL Server raises 'conversion of a varchar data type to a datetime data type resulted in an out-of-range value' -- an opaque whole-transaction rollback WITHOUT the attribute name and DN that _ts()'s own abort exists to supply. Separately the same function silently zeroes the time on any GeneralizedTime shorter than 14 digits: '201905151234Z' (legal minute precision) -> '2019-05-15 00:00:00', losing 12:34 with no warning, because the date-only branch tests len>=8 rather than exactly 8.
  - Fix: Replace the positional test with datetime.datetime.strptime(text, '%Y-%m-%d %H:%M:%S') in a try/except -- one line, and it rejects 0000-00-00, month 13 and Feb 29 of a non-leap year. Tighten the date-only branch to a raw value of exactly 8 digits (optionally with a trailing Z) so a 10-, 12- or 13-digit GeneralizedTime aborts loudly instead of being silently rounded to midnight.

- **[correctness]** A single unparseable GeneralizedTime aborts all eight sources with no override, and the multi-value warning meant to measure the assumption cannot say which table it saw. Failure scenario as in the regressions list: one weillCornellEduStartDate reading '0', 'N/A' or 'unknown' -> SystemExit out of ed_sors_sor_record() -> run_sources() unwinds -> canonical, sor_role_record, organization, email, phone, location and location_master are never written either, every night. No IA_* flag degrades it, though the two other new guards in the same slice both got one. Related: _TS_MULTI_WARNED is keyed on the attribute name alone and is module-global, so createTimestamp coming back multi-valued in canonical permanently suppresses the identical warning for sor_record (and for every other slice's use of _ts), and the message reports the value count of the first offending entry only, never how many entries were affected -- so the claim that it says 'the assumption was violated and by how much' is not what the code does.
  - Fix: Make the bad-value path a skip-and-count, not a process kill: emit '' for that attribute on that row, increment a per-attribute counter, and abort at the end of the source only above a threshold (or gate the abort on IA_STRICT_TIMESTAMPS, default off for the first runs). Log the offending DNs at WARNING -- never the values. Key _TS_MULTI_WARNED on (label, attr) and log a total at the end of each source.

- **[correctness]** The sor_record structural floor is an invented threshold presented as a structural impossibility, and it hard-aborts with no override. The code states sor_record 'can never legitimately be the smaller of the two' because 'every person carries at least one SOR record'. The only support is PORT_SPEC.md:298, which is prose in a divergence note, not a measurement -- and the two counts being compared are taken after DIFFERENT filters the same commit introduced: canonical keeps CWID-bearing persons with max uid length < 20, sor_record keeps CWID-bearing SOR records with max uid length > 20. Nobody has measured how many SOR records carry a uid of 20 characters or fewer; that is exactly what the new uid_excluded counter exists to find out. Failure scenario: the uid>20 filter (default ON) excludes a material share of ou=sors, or ou=people legitimately holds person entries with no SOR record (retired or service CWIDs); len(out) lands below canonical's count and the run SystemExits, taking every other source with it, every night, with no env flag to proceed. The two other floors in the same function are correctly conditional; this one is not.
  - Fix: Downgrade the sor_record >= canonical check to logger.warning until one run has printed both counts, or gate the SystemExit on IA_STRUCTURAL_FLOOR=1 the way IA_MIN_ROWS_SOR_RECORD is gated. Restate the premise in the message as an expectation from PORT_SPEC.md:298 rather than a structural guarantee, and cite the line.

- **[correctness]** The orgUnit migration the port was commissioned for is still unguarded, and against the assembled writer it fails worse than the finding predicted. With PREFER_ORGUNIT off (the default) _orgunit_value() returns the OLD attribute alone. When ED retires weillCornellEduDepartment / DepartmentCode / PrimaryDepartment / PrimaryDepartmentCode, mv() returns '', _null_if_blank() (/Users/paulalbert/worktrees/reciterdb-ia/update/_ia_sources_draft.py:966) turns '' into None, and the MERGE's COALESCE(src.c, tgt.c) keeps the last pre-migration value forever. The columns therefore do not go empty as the finding said -- they FREEZE at a stale value that looks correct: no row-count change, no NULL to notice, and the only signal an INFO line reading old=0 new=N in a nightly cron log. Failure scenario: ED completes the migration in month 1; _person_canonical.weillCornellEduDepartment still shows everyone's month-0 department in month 9; every consumer that joins on department is silently wrong and nothing in the job has failed.
  - Fix: Make the watch a guard, not a log line. After each source's loop, raise the orgunit line to logger.warning when old_n falls below (say) half that source's emitted row count for the base, and abort when old_n is 0 while new_n is non-zero -- that is the migration having completed underneath a build that is not yet reading the new attribute. Fold the same numbers into the DDL warning so an operator sees one WARNING, not four INFOs.

- **[correctness]** The restored canonical uid filter does not reproduce the SPL for uid-less entries, though the UID_LEN_FILTER comment claims 'the port pushes exactly the population the Splunk job has always pushed'. _uid_len() returns max((len(v) for v in values), default=0), so an entry with no uid scores 0. Verified: uids=[] -> (0, 0) -> 0 >= 20 is False -> canonical KEEPS the row. Splunk's `where isnotnull(dn) and len(uid) < 20` (L180, L189) evaluates len(null) to NULL and `where NULL < 20` drops the row, so such a person has never reached _person_canonical. Failure scenario: an ou=people entry whose uid is unreadable under this job's bind (or genuinely absent) but which carries a CWID is now INSERTed into _person_canonical with uid NULL -- an insert, so no row-count diff flags it, the same invisibility class as the finding the filter was restored to close. The sor_record side is correct by luck: 0 <= 20 excludes it, matching Splunk.
  - Fix: Return None rather than 0 from _uid_len() when row.all('uid') is empty and treat that as excluded on both sides, or add `if not uid_count: continue` alongside the CWID test with its own counter. Then the claim in the UID_LEN_FILTER comment is true as written.


## Slice: roles-org (NEEDS_FIXES)

### Regressions introduced by the fix round

- Both source docstrings now assert a runtime protection that has no production caller. ed_sor_role_record(): "assert_dn_overlap() is what proves the stored keys agree with it, before any write and independently of --no-delete, so the re-key cannot reach the upsert as a silent duplicate-insert." ed_organization(): "assert_dn_overlap() is what decides whether the stored keys agree, before any write: this is the type it was written for." Grep across buildIdentityAuthority.py and _ia_sources_draft.py finds assert_dn_overlap called only from demo() (lines 371 and 377) -- never from either main(). The organization DN re-key is the highest-risk item in this slice (the SPL builds the key from seeAlso, which the code itself says "may have been writing wrong DNs for years"), and its stated mitigation does not run.

- The same fix DELETED the draft's actionable manual precondition and replaced it with that false assurance. The draft's ed_organization() docstring read: "PORT_SPEC.md lists that as an open question; it is worth a diff of the old table against this source's keys before DELETE_MODE is flipped." That sentence is gone from the fixed version. Net effect: an operator reading the fixed docstring believes the check is automatic and skips the manual diff the draft told them to perform.

- --no-delete no longer works as the documented safe first-run path. The draft's roles-org sources had no DB dependency at all; the fixed ones abort the whole build whenever the target table's row count exceeds the ED read by more than 2%, which is the expected state of both tables per the module's own docstring (lines 35-37: the Splunk delete lane under-deletes) and per the dbside finding that soft-delete flags can never be cleared. The canonical slice's remediation for the DN re-key explicitly requires that first live run to be --no-delete; it now aborts, with no env override and no code path that permits it.

- Build-only and --dry-run runs of these two sources now require IA_DB_URL / IA_DB_USERNAME / IA_DB_PASSWORD, and each source opens a second pymssql connection of its own rather than reusing db_dns()'s. The author declares the credential half in the deferred list, but not that _assert_target_schema() issues its own COUNT(*) against the same table db_dns() is about to read in full -- two connections and two full scans of _person_sor_role_record per run where one would do.

### Still unresolved

- **[correctness]** _assert_target_schema() compares SELECT COUNT(*) (total rows) against len(out) (DISTINCT entry DNs). These count different things, and db_dns() in the dbside slice explicitly warns that the dn column may not be unique ("%s has %d duplicate/blank %s values -- the upsert key is not unique there"), while ed_organization()'s own docstring says the SPL's reconstructed key "collided across siblings -- an org with two cn values produced two rows". Failure scenario, computed: _organization holds 2,600 rows over 2,500 distinct dns after years of `outputlookup append=t`; ED returns all 2,500 live orgs; shortfall = (2600-2500)/2600 = 3.85% > MAX_DELETE_FRACTION -> SystemExit inside ed_organization(), which is inside run_sources(), so the entire nightly job for ALL EIGHT types stops, every night, with a message asserting "the read was truncated" when the read was complete. With COUNT(DISTINCT dn) the same inputs give 0.00%.
  - Fix: Count what the reconcile counts: `SELECT COUNT(DISTINCT [dn]) FROM [t] WHERE [dn] IS NOT NULL` -- the exact number db_dns() already derives as len(dns). Better, take the number from db_dns() rather than issuing a second, differently-shaped query.

- **[correctness]** The shortfall guard's premise contradicts the module's own docstring. buildIdentityAuthority.py:35-37 states that in the Splunk job "source='ed' means 'seen in ED at some point since the lookup was last truncated', not 'currently in ED'" -- i.e. the delete lane under-deletes and the target tables accumulate rows for entries long gone from ED. The guard treats exactly that accumulation as evidence of a truncated read. Computed: at 3% stale the guard aborts (3.00% > 2.00%); at 5% and 10% likewise. Worse, it reproduces the dbside reviewer's cumulative-soft-delete failure on a second path -- COUNT(*) still counts rows the never-cleared `deleted` flag has soft-deleted, so on a 30k table with 0.4% churn/run the guard first aborts on run 6 at 2.40% (identical to their simulation) and every run after. There is no override (the author declined one deliberately), so the job is unrunnable until someone edits the source. Critically, the author's claim that this "adds no refusal the run did not already have" is false for --no-delete, which is precisely the path the canonical slice's own remediation mandates: "force the first live run to --no-delete". (For the delete-enabled path the claim does hold -- algebraically the guard fires iff G/S > 0.02 + N/S, strictly weaker than plan_deletions().)
  - Fix: Make this a WARNING carrying both counts, not a SystemExit, until one measured run establishes the real gap between ED and each stored table -- which is the number the finding asked for in the first place. If it must abort, gate it on its own named constant (e.g. MAX_READ_SHORTFALL) set from that measurement, and honour an explicit IA_ALLOW_SHORTFALL=<type> opt-in so the mandated --no-delete first run can proceed. Also confine the blast radius: raising inside a source kills all eight types, so collect the shortfall per type and decide after run_sources().

- **[correctness]** The unresolved-base-DN finding is only half fixed, and the half that matters is not in this slice. Passing alias= sets fn.alias, but the guard that consumes it lives in main(), and in the concatenated module the dbside slice's main() (_ia_sources_draft.py:1339) redefines main() AFTER the scaffolding's, still as `UNRESOLVED_ALIASES & {fn.type_name for fn in SOURCES.values()}`. Verified: the last `def main` wins on concatenation. So in the assembled module the warning is still dead and ed_organization() still runs against the unproven ou=departments,ou=Groups base with no log line -- the exact failure the finding described (binds, returns 150 rows, clears the floor of 100, operator told nothing). Separately, the guard's `if fn.alias` filter silently exempts any source that forgets alias=, so a full-assembly result of exactly {'ed-groups'} -- the author's own verification value, obtained on a two-source module -- would in the real module mean ed-emails, ed-phones and ed-locations are unresolved and unwarned.
  - Fix: Warn where the alias is actually used, not where main() happens to be defined: `if alias in UNRESOLVED_ALIASES: logger.warning(...)` inside ldap_search(), which no slice can shadow. Keep the alias= arguments. Add the demo() assertion the contact review asked for, and make it assert that EVERY registered source has a non-None alias, so an alias-less source is a failure rather than an exemption.

- **[correctness]** MIN_ROWS is explicitly untouched, so the finding's stated defect ("neither floor can catch a large partial read") is unchanged on every run before someone reads a log line and edits the file. sor_role_record remains 20000 against a population the port's own PORT_SPEC.md:298 calls "the highest-cardinality object in the pipeline", and organization remains 100. The replacement guard does not close the gap it was offered for: because it fires only when stored exceeds built, a read that returns 25,000 of ~100,000 role records into a table that is itself only ~25,000 rows (the SPL pushes on a 2-day modifyTimestamp window, so the DB is not a complete mirror) yields shortfall 0% and passes both guards.
  - Fix: Run the two COUNT(DISTINCT dn) queries once and hard-code the measured floors with the date and number in a comment, as the finding asked. The new logging is a good addition but is not a substitute -- MIN_ROWS is what run_sources() actually enforces.

- **[correctness]** weillCornellEduStartDate / weillCornellEduEndDate are silently reformatted for the whole column on an unmeasured assumption. The finding's fix text made the measurement a precondition ("confirm the target column types with one query ... plus one sample of the existing values, so the port writes the shape already in the column rather than a third shape"); only the code half was applied. Verified in the harness: a raw '20240630235959Z' becomes '2024-06-30 23:59:59'. The SPL never sliced these two fields, so whatever ldapfilter rendered is what _person_sor_role_record holds today. If that column is NVARCHAR holding '20190701000000Z' or '2019-07-01T00:00:00', the fix rewrites every row to a new shape and breaks every downstream LEFT(col,10) / strptime / string comparison -- with no row-count change to expose it. _gtime() also drops any second value via row.get() without logging, so a role record with two start dates loses one invisibly.
  - Fix: Run `SELECT column_name, data_type FROM information_schema.columns WHERE table_name='_person_sor_role_record' AND column_name IN ('weillCornellEduStartDate','weillCornellEduEndDate','createTimestamp')` plus `SELECT TOP 5 weillCornellEduStartDate FROM _person_sor_role_record` BEFORE the first run, and write the shape that comes back. Add a one-line `if len(row.all(attr)) > 1: logger.warning(...)` in _gtime() so the one remaining first-value-wins read is not silent.


## Slice: contact (NEEDS_FIXES)

### Regressions introduced by the fix round

- ed_phones_contact() froze _contact_phone.telephoneNumber for the mobile-only population. The draft's condemned `r.get("telephoneNumber") or r.get("mobile")` at least kept that column refreshed with a live value; the fix binds it NULL for those entries and _merge_sql's COALESCE then preserves the pre-cutover value permanently, un-refreshable and un-clearable. The draft lost the SECOND number on dual-type entries; the fix makes the FIRST number stale forever on single-type mobile entries -- a larger population, and one no row-count diff can see.

- ed_phones_contact() now emits a column (`mobile`) that the authoritative SPL's table clause (Splunk Queries - 2026-09-05.txt:521) says _contact_phone does not have. The draft's emitted key set was exactly the writable one for all four tables; the fixed slice's is not, and _columns_for()/_merge_sql() will name `mobile` in the MERGE -- one "Invalid column name" error rolls back the single transaction covering all eight tables. --dry-run cannot catch it because it logs the statement without executing it.

- The IA_LOCATION_MASTER_MIN_ROWS gate turned a weak floor on one source into a hard stop on the whole job. Verified by running the assembled module: with the variable unset (no manifest sets it) and the exact `--dry-run` command kubernetes/k8-cronjob-identity-authority.yaml:56 ships, the run does a full ed-emails LDAP read and then SystemExits before any of the other seven sources build. The draft ran end to end. The protection is aimed at a destructive write that --dry-run cannot perform.

- The new `if not desk or not cell` abort in ed_phones_contact() can halt all eight sources on a perfectly healthy read -- if ED keeps every number in `telephoneNumber` and uses weillCornellEduType only as a label, `cell` is 0 and the job stops, with a message naming three causes none of which is the real one. The draft had no such trigger, and the premise behind the new one is the very thing the finding asked to be measured first.

### Still unresolved

- **[data-loss]** ed_phones_contact() now emits telephoneNumber="" for every mobile-only entry (verified: an entry with only `mobile` yields {'uid':'b','mobile':'CELL2','weillCornellEduType':'mobile'} and telephoneNumber empty). The dbside writer turns "" into None (`_null_if_blank`, update/_ia_sources_draft.py:966) and `_merge_sql` sets `tgt.[telephoneNumber] = COALESCE(src.[telephoneNumber], tgt.[telephoneNumber])` (update/_ia_sources_draft.py:1038) -- its own docstring says "a value can be replaced but never cleared". Every row already in _contact_phone that came from the SPL's mobile branch holds that person's mobile number in telephoneNumber (SPL L513 `rename mobile as telephoneNumber`). After cutover that value is bound NULL on every run, so COALESCE preserves it forever: it can never be refreshed and can never be cleared. Failure scenario: a person changes their mobile number on 2026-10-01. ED updates the `mobile` attribute; the port writes the new number into the new `mobile` column and NULL into telephoneNumber; every existing consumer of _contact_phone.telephoneNumber (the only phone column that has ever existed) keeps serving the number that was correct in September, indefinitely, with no row-count change and no error. The draft's `r.get("telephoneNumber") or r.get("mobile")` -- the thing the finding condemned -- did not have this property.
  - Fix: The `ALTER TABLE _contact_phone ADD mobile` note is not a sufficient prerequisite. Either (a) add the one-time clear to the prerequisite and state it as load-bearing: `UPDATE _contact_phone SET telephoneNumber = NULL WHERE weillCornellEduType LIKE '%mobile%' AND weillCornellEduType NOT LIKE '%telephoneNumber%';` run in the same change window as the ADD COLUMN, or (b) keep writing the mobile value into telephoneNumber as well as into `mobile` so the legacy column stays live. Do not rely on the upsert to converge -- with COALESCE it provably cannot.

- **[correctness]** ed_phones_contact() emits a `mobile` key for which there is no evidence of a target column. The SPL's table clause at Splunk Queries - 2026-09-05.txt:521 -- the only description of _contact_phone's shape in the authoritative source -- names telephoneNumber and not mobile. The dbside writer takes the union of emitted dict keys (`_columns_for`, update/_ia_sources_draft.py:983) and names them in the MERGE, and `upsert()` wraps all eight tables in one transaction with a rollback on any exception (update/_ia_sources_draft.py:1061-1123). Failure scenario: first live run, SQL Server raises "Invalid column name 'mobile'" on the _contact_phone MERGE; upsert() rolls back; NOT ONE of the eight tables is written, and this repeats every night until someone runs the DDL. It is invisible to every pre-flight the repo has: --demo never builds SQL for phone, and --dry-run logs the statement without executing it (update/_ia_sources_draft.py:1088-1098), so three green dry runs precede the failure -- the same trap _merge_sql's docstring says the sibling port already fell into once. The finding's own Fix offered a no-DDL alternative (pipe-join both attributes into the existing telephoneNumber column); the chosen alternative is legitimate but its prerequisite is enforced nowhere.
  - Fix: Do not leave the prerequisite to a writer slice that does not exist yet. Either land the ALTER first and record the verification (`SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='_contact_phone'`, output pasted into the docstring), or gate it in the source itself for now -- but the honest minimum is to stop describing this as "deferred to the writer" while the shipped manifest runs the job nightly.

- **[correctness]** The IA_LOCATION_MASTER_MIN_ROWS gate aborts the entire eight-source job, including --dry-run, and nothing sets the variable. kubernetes/k8-cronjob-identity-authority.yaml:56 ships `command: ["python3", "buildIdentityAuthority.py", "--dry-run"]` and its env block (line 63-65) sets IA_DELETE_MODE only. Verified by running the assembled module with the env unset and --dry-run: the job issues a full ed-emails LDAP read, then raises `ABORT: IA_LOCATION_MASTER_MIN_ROWS is unset or not a positive integer`. run_sources() has no per-source skip, so email/phone/location and the four non-contact sources never build. Failure scenario: 07:00 UTC nightly, the CronJob burns a full contact-email crawl, exits 1, retries twice on backoffLimit, and the parallel-run-against-Splunk reconciliation that the manifest's own comments describe as the cutover plan never produces a single diff. The destructiveness argument does not apply on --dry-run, where by construction nothing can be written. The docstring's "Checked before the search so an unmeasured run costs nothing" is also only true of its own search -- ed_emails_contact sorts first and has already run.
  - Fix: Add `- name: IA_LOCATION_MASTER_MIN_ROWS` to kubernetes/k8-cronjob-identity-authority.yaml with the measured value (the slice created the requirement; wiring it is part of the slice), and make the gate a warning rather than a SystemExit when no write path is live -- e.g. read the same IA_* env convention DELETE_MODE uses, or fall back to MIN_ROWS with a loud warning and abort only when writes are enabled.

- **[correctness]** Finding 4 (the four-table re-key) is answered by asserting that the mitigation runs; it does not. The slice header states "assert_dn_overlap() does exactly that, before any write and independently of --no-delete, and aborts below 90%", and the author's claim repeats it. grep on update/buildIdentityAuthority.py returns assert_dn_overlap at line 255 (the def) and lines 371 and 377 -- both inside demo(). main() (lines 407-431) never calls it, and the draft's main() (update/_ia_sources_draft.py:1326-1361) never calls it either. Failure scenario: first run with --no-delete, the flag an operator reaches for first. plan_deletions is skipped by design, assert_dn_overlap is never invoked because it is not in the code path, and upsert() runs with `ON tgt.[dn] = src.[dn]` where the stored dn is `uid=<uid>,ou=telephoneNumbers,ou=contacts,ROOT` (SPL L520, one key per person) and the live dn is a real per-entry DN. Nothing matches, WHEN NOT MATCHED INSERTs fire for every row, and _contact_phone / _contact_email / _contact_location / _location each gain a full parallel copy under the new key shape -- exactly the outcome the finding described, with the stale old rows keeping their frozen values. Keeping entry_dn keying was the instruction; presenting an uncalled function as the runtime guard is what makes this still open.
  - Fix: Either wire the call (`assert_dn_overlap(built, existing)` immediately after db_dns() and before the --no-delete branch in main()) as a scaffolding change, or downgrade the slice header from "runs before any write" to "is defined but not yet on the code path -- wiring it is a prerequisite of the first live run" and list it with the other deferred prerequisites. Do not leave a docstring asserting a guard that grep disproves.

- **[correctness]** The new phone half-empty abort measures something other than what it cites, and fails closed on an unmeasured premise. PORT_SPEC.md:249 asks for the two FILTER ARMS to be counted ("Log `ldap ed-phones telephoneNumber: N` and `ldap ed-phones mobile: M` separately, abort if either is 0") -- i.e. entries matching weillCornellEduType=telephoneNumber vs =mobile. The code instead counts ATTRIBUTE presence: `desk = sum(1 for r in rows if r.all("telephoneNumber"))`, `cell = sum(1 for r in rows if r.all("mobile"))`. These differ, and the code's own docstring says why the difference is unknowable in advance: the SPL's mobile arm is a streaming ldapfilter inside an append subsearch that "may always have returned nothing", so nobody has ever observed whether ED populates a `mobile` attribute in ou=telephoneNumbers,ou=contacts. Failure scenario: ED stores every contact-phone number in `telephoneNumber` and distinguishes desk from cell only through weillCornellEduType (a common directory shape). cell == 0 on a completely healthy read, SystemExit fires, and all eight sources stop -- with a message that blames "a changed weillCornellEduType value, a wrong base DN or a half-populated replica" and never mentions the actual cause. The port would otherwise have been correct in that scenario, with `mobile` simply empty.
  - Fix: Count what PORT_SPEC L249 names, from the same read: `desk_arm = sum(1 for r in rows if 'telephoneNumber' in r.all('weillCornellEduType'))` and `cell_arm = sum(1 for r in rows if 'mobile' in r.all('weillCornellEduType'))`, and abort only when one of those is zero (that genuinely proves a dead half of the OR'd filter). Keep the attribute-presence counts, but log them as the measurement they are -- a zero `mobile` attribute count is a finding about ED's shape, not a reason to stop the job.

- **[correctness]** The safety argument for expanding mv() from 2 sites to 36 is stated as established fact and is not measured. The slice header asserts "Every other multi-valued attribute has been arriving newline-joined all along", making mv() "a declared separator change (newline -> pipe) and nothing more". That holds only if Splunk's dbxoutput wrote the whole joined multivalue into the column; if DB Connect wrote first-value-only -- a normal behaviour for a DB writer handed an mv field, and unverified anywhere in the three source documents -- then mv() is a value change across 36 columns in 4 tables, which is precisely the change the author declined to make for weillCornellEduPrimaryEntry on the grounds that it "would change values no finding asked to change". Failure scenario, and it is not a per-row one: _merge_sql CASTs each parameter to NVARCHAR(MAX) and lets the target column's own type drive the conversion (update/_ia_sources_draft.py:1037). A person with two roomNumber values, or a location with two postalCode values, produces "10021|10065" against a narrow column; SQL Server raises 8152 "String or binary data would be truncated"; upsert()'s except clause rolls back the single transaction covering all eight tables. One over-width value anywhere in four tables means nothing is written that night.
  - Fix: Take the measurement the header already promises before the first live write, not after: for the 36 columns, `SELECT COLUMN_NAME, DATA_TYPE, CHARACTER_MAXIMUM_LENGTH FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME IN ('_contact_email','_contact_phone','_contact_location','_location')` plus `SELECT COUNT(*) FROM _contact_email WHERE mail LIKE '%' + CHAR(10) + '%'` (a non-zero count proves the newline-joined premise; zero disproves it). Restate the header claim as an inference pending that query, and note that the failure mode of an over-width value is a whole-transaction rollback rather than a truncated field.


## Slice: dbside (NEEDS_FIXES)

### Regressions introduced by the fix round

- MAX_DELETE_FRACTION is now enforced on normalised KEYS but the plan is expanded to stored ROWS, so the ceiling can be exceeded silently. The draft's plan_deletions() returned stored DN strings 1:1, so len(gone) and rows-flagged were the same number; the new key/originals indirection in main() (`plan[t] = [dn for key in keys for dn in originals[t].get(key, [])]`) breaks that identity. Reproduced: _contact_email with 10,005 live keys plus 450 duplicate stored rows differing only in case; 150 keys gone = 1.50%, plan_deletions logged 1.50% and passed the 2.00% ceiling, but apply_deletions issued UPDATEs naming 600 stored rows = 5.74% of the table. db_dns() itself warns these collisions exist, so this is not hypothetical. Fix: re-check the ceiling in main() AFTER expansion (or size `existing` in stored rows, not keys), before upsert() runs.

- The upsert lost its all-or-nothing property across tables (one transaction over all eight in the draft -> conn.commit() per table). This is disclosed in the deferred notes and traded deliberately for the HOLDLOCK finding, so it is not hidden -- but two aggravating facts are not disclosed: (a) the justification rests on the ~2,300-statement figure produced by the phantom 2100-parameter cap (unresolved item 5), so the trade may be entirely unnecessary; and (b) it converts the three post-commit aborts in unresolved item 1 from harmless into genuine partial-write outcomes, each announced with a false 'Nothing has been written'. Verified: 7 of 8 tables committed before a trivial config-level abort.

- demo() now encodes a false belief as a passing assertion ('the key placeholder must be bare: casting it costs the index seek'). The draft made no such claim. A test that asserts a non-fix is worse than no test -- it will keep the next reviewer from re-checking the seek. Detail in unresolved item 6.

### Still unresolved

- **[correctness]** PARTIALLY-FIXED FINDING (apply_deletions ordering). The fix round hoisted ONE of the three pre-write checks ahead of the write (the `flags.get(t) is None` block in main()) and left the other two behind it. Reproduced by driving main() against a fake pymssql: (a) IA_DELETE_MODE=delete with IA_DELETE_CONFIRMED unset -> 135 MERGE statements execute, all 8 tables COMMIT, then SystemExit '...set IA_DELETE_CONFIRMED=yes. Nothing has been written.' commits=8, rollbacks=0. (b) IA_DELETE_MODE typo'd as 'Flag' -> identical: 135 MERGEs, 8 commits, then 'ABORT: DELETE_MODE=Flag is neither flag nor delete'. Worse, that casing typo also silently disables both data-loss fixes this round added (db_dns' flagged-row exclusion and upsert's flag clear are both gated on `DELETE_MODE == "flag"`), so the job reports failure nightly while writing successfully with the fixes inert. (c) Both fire even under --no-delete, where plan={} yet apply_deletions is still called and still aborts post-commit. (d) upsert()'s own NOT-NULL/non-numeric flag-column abort sits inside the per-table write loop: with _person_sor_role_record.deleted NOT NULL char(1) (last alphabetically) the run commits canonical, email, location, location_master, organization, phone, sor_record -- 104 MERGEs, 7 commits -- then SystemExit '...Nothing has been written.' The rollback log line correctly names the 7 committed tables, so the module contradicts itself in the same stderr. A false 'Nothing has been written' after 8 committed tables invites a restore of tables that were fine.
  - Fix: Hoist all of it into the pre-flight block in main() that already runs after db_dns() and before upsert(): validate DELETE_MODE membership and the IA_DELETE_CONFIRMED gate there unconditionally (not gated on --no-delete), and resolve the clear literal for every type there too -- `flags` is fully known at that point, so the NOT NULL/non-numeric abort needs no state from inside the loop. Pass a resolved {type: clear} map into upsert() instead of deriving it per table. Then reword every abort reachable after the first conn.commit(): 'Nothing has been written' must become 'committed before the failure: <types>'.

- **[data-loss]** UNCLEARABLE FLAG (independent). The flag clear added by this round is gated on `DELETE_MODE == "flag"` in upsert(), and db_dns' flagged-row exclusion is gated the same way. Verified: DELETE_MODE=delete runs to completion OK with 'merge clears flag: False | read excludes flagged: False'. DELETE_MODE ships as 'flag' and the module's stated end state is to flip to 'delete' once conf-db_outputs is read. On that flip every row soft-flagged during the flag-mode period is never un-flagged by anything, ever: it is present in ED, the MERGE matches and updates it, and `deleted` stays set. Consumers filtering WHERE deleted IS NULL lose those people permanently, with no row-count change on either side -- the identical silent shape the finding was raised to close, relocated behind a mode switch. Nothing logs or warns at the moment of the flip.
  - Fix: Decouple the clear from the deletion mode. Presence in ED is a statement about the row, not about how deletions are written: run the clear whenever the column exists (`info is not None`), and gate only the WRITING of the marker on DELETE_MODE=='flag'. If the clear really must be off under 'delete', main() has to abort on the transition, naming the count of rows that would be stranded (COUNT(*) WHERE deleted IS NOT NULL), and require a one-time explicit acknowledgement.

- **[correctness]** DEAD GUARD, AGAIN (finding: main()'s unresolved-alias intersection). The intersection was moved to `fn.alias`, but the comprehension filters alias-less sources out: `UNRESOLVED_ALIASES & {fn.alias for fn in SOURCES.values() if fn.alias}`. Every @source call in /Users/paulalbert/worktrees/reciterdb-ia/update/_ia_sources_draft.py -- @source("canonical"), @source("email"), @source("phone"), all eight -- omits alias=. Reproduced by registering two alias-less sources against the assembled module: 'aliases recorded: {ed_emails_contact: None, ed_phones_contact: None}', 'guard fires?: False'. So the live run still proceeds against the five unproved base DNs in silence, now behind code that reads as fixed. The slice's demo() added nine new assertions and not the one that would catch this, and --demo returns before main(), so nothing exercises the guard at all.
  - Fix: dbside must defend itself rather than trust the sibling slices. Add to demo(): `missing = sorted(n for n, fn in SOURCES.items() if not fn.alias); assert not missing, missing`. One line, and it fails the moment a slice lands without alias=. Separately confirm each fixed source slice passes alias= (canonical/sor_record -> ed-people/ed-sors, email -> ed-emails, phone -> ed-phones, location -> ed-locations, location_master -> ed, organization -> ed-groups).

- **[correctness]** IA_ALLOW_EMPTY_TABLE collapses two independent guards into one knob, re-opening the hole the floor finding asked to close. In db_dns() the waiver is checked FIRST and short-circuits both the empty-read abort and the below-floor abort. Because DB_MIN_ROWS is `dict(MIN_ROWS)` -- ED source floors, admittedly never measured against the IA tables -- hitting a false floor is likely, and the waiver is the only documented escape. Reproduced: _contact_location legitimately holds 4,800 rows against a floor of 5,000; operator sets IA_ALLOW_EMPTY_TABLE=location; run passes. A later run where that read returns ZERO rows also passes: upsert still executes, `existing` is empty so assert_dn_overlap() skips the type (`if not existing: continue`) and plan_deletions() skips it too, so every real deletion for that type is silently suppressed AND the DN-duplication guard is off for it -- no abort, no warning beyond the one line saying the floor was waived.
  - Fix: Split the knob. Keep IA_ALLOW_EMPTY_TABLE for a genuinely new table (waives the empty-read abort only), and add a separate IA_DB_MIN_ROWS override (type:count pairs) for calibrating a floor. An empty read stays an abort unless the empty waiver is set for that specific type. Then run the eight COUNT(*)s and replace `DB_MIN_ROWS = dict(MIN_ROWS)` with measured numbers, which the deferred note already commits to.

- **[correctness]** INVENTED THRESHOLD PRESENTED AS A REAL SERVER LIMIT. The header states 'TDS caps a statement at ~2100 parameters, so _batch_size() honours both... Both are kept because both are real server limits', and _batch_size() is `2000 // (len(columns) + 1)`. That cap does not exist for this driver. Verified from pymssql 2.4.0 source: Cursor.execute -> execute_query -> format_sql_command -> _substitute_params, and _quote_simple_value renders a Python str as N'...' (_mssql.pyx:2073-2074). Every value is interpolated into the batch text client-side; zero TDS parameters are ever bound. _merge_sql's own docstring asserts exactly this three paragraphs later ('pymssql interpolates parameters client-side, so a None arrives as a bare NULL literal') -- the two statements cannot both be operative. Consequence: a 45-column type batches at 43 rows instead of the genuine 1000-row VALUES cap, ~931 MERGEs for _person_sor_record instead of ~40. That ~23x inflation is the SOLE cited justification for surrendering the cross-table transaction (see regressions), so a threshold that does not apply is now load-bearing for a real loss of transactional scope.
  - Fix: Drop the parameter term. Bound the batch on the two limits that are real for interpolated SQL: MERGE_MAX_ROWS=1000 (the T-SQL VALUES row-constructor cap, genuine) plus an approximate rendered-statement byte budget. Re-measure the statement count, then decide whether the per-table-commit trade is still needed. Correct the header so it stops asserting a cap this driver makes unreachable.

- **[correctness]** THE UN-CAST KEY DOES NOT RESTORE THE INDEX SEEK, and demo() now asserts that it does. The finding was that `ON tgt.[dn] = CAST(%s AS NVARCHAR(MAX))` loses the seek on a varchar column; the fix leaves the key placeholder bare and locks it in with `assert "USING (VALUES (%s, CAST(%s AS NVARCHAR(MAX))" in sql, "the key placeholder must be bare: casting it costs the index seek"`. But pymssql emits a bare %s str param as N'uid=...' (_mssql.pyx:2073-2074, verified), so src.[dn] is still nvarchar -- only NVARCHAR(n) rather than NVARCHAR(MAX). nvarchar still outranks varchar in data-type precedence, so on a varchar dn column the optimizer still emits CONVERT_IMPLICIT(tgt.[dn]) and the seek is still lost. Failure scenario unchanged from the original finding: _person_sor_record.dn varchar(255), ~40k rows at 43 rows/batch = ~931 MERGEs each scanning the table, under HOLDLOCK. The only thing genuinely removed is the LOB-ness of MAX (a real improvement, but not the claimed one) -- and the new assertion will stop the next reviewer from re-checking.
  - Fix: Resolve the key column's declared type once in db_dns() -- the _flag_column() INFORMATION_SCHEMA query is already there, one more row is free -- and cast the key to match: CAST(%s AS VARCHAR(n)) when dn is varchar/char, bare or NVARCHAR(n) when it is nvarchar. Change demo()'s assertion and message from 'must be bare' to 'must match the stored key's type', asserting both branches.

